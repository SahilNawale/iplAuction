Use the following process to run the website : 
  - Create a virtual environment and install the required dependencies
  - run npm build 
  - run python manage.py collectstatic for production version 
  - change the backendurl in axiosinstance file according to the domain 
  - unzip the media folder to get the player images
  - run npm install to install the dependencies
